% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/concatenation_sim2.R
\name{concatenation_sim2}
\alias{concatenation_sim2}
\title{Fusionne des fichiers CSV.gz en un seul fichier via DuckDB}
\usage{
concatenation_sim2(
  input_dir,
  output_file,
  delim = ";",
  format = c("parquet", "csv")
)
}
\arguments{
\item{input_dir}{Character. Chemin vers le dossier contenant les fichiers \code{.csv.gz}.}

\item{output_file}{Character. Chemin complet vers le fichier de sortie.}

\item{delim}{Character. Séparateur des fichiers CSV. Par défaut \code{";"}.}

\item{format}{Character. Format de sortie : \code{"parquet"} (par défaut) ou \code{"csv"}.}
}
\value{
Character. Chemin du fichier généré (retourné invisiblement).
}
\description{
Cette fonction lit tous les fichiers compressés au format \code{.csv.gz}
présents dans un dossier, les concatène, puis écrit le résultat
dans un fichier de sortie (CSV ou Parquet). La lecture et l'écriture se font en streaming via DuckDB,
ce qui permet de traiter de très gros volumes de données
(plusieurs dizaines de Go) sans saturer la mémoire RAM.
}
\examples{
\dontrun{
concatenation_sim2(
  input_dir = "C:/workspace/sim/",
  output_file = "C:/workspace/sim/ALL_sim2.parquet",
  delim = ";",
  format = "parquet"
)
}

}
